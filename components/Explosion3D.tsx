// versao mobile — a explosao no celular ja tem flash, tremida e haptics
// o cogumelo em canvas so roda na web (Explosion3D.web.tsx)

type Props = { active: boolean; delayMs?: number };

export default function Explosion3D(_props: Props) {
  return null;
}
